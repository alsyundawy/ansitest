# Ansible Collection - ansible.default

Documentation for the collection.

This is ansible code that was written by kneutron, yaml and shell / bash scripts

1) Run the takeover script on the target - the .sh should handle Debian / RHEL-derived distros
2) Add the target instance name to /etc/ansible/hosts on the controller under appropriate stanza group
3) Test ping - the .sh scripts call the ansible .yaml / .yml for you
